import { FiltroempresaPipe } from './filtroempresa.pipe';

describe('FiltroempresaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroempresaPipe();
    expect(pipe).toBeTruthy();
  });
});
