import { FiltconceptoPipe } from './filtconcepto.pipe';

describe('FiltconceptoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltconceptoPipe();
    expect(pipe).toBeTruthy();
  });
});
