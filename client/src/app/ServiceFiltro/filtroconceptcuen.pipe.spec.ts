import { FiltroconceptocuentaPipe } from './filtroconceptocuenta.pipe';

describe('FiltroconceptcuenPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroconceptocuentaPipe();
    expect(pipe).toBeTruthy();
  });
});
