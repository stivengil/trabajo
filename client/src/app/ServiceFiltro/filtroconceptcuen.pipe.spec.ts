import { FiltroconceptcuenPipe } from './filtroconceptcuen.pipe';

describe('FiltroconceptcuenPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroconceptcuenPipe();
    expect(pipe).toBeTruthy();
  });
});
