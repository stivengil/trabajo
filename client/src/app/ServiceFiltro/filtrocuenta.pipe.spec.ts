import { FiltrocuentaPipe } from './filtrocuenta.pipe';

describe('FiltrocuentaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrocuentaPipe();
    expect(pipe).toBeTruthy();
  });
});
